[pytest]
# Benchmark files/functions are named bench_*, mirroring the C++ suite.
# bench_fw/ is excluded via conftest.py collect_ignore_glob.
python_files = bench_*.py
python_classes = Bench*
python_functions = bench_*
addopts = --benchmark-sort=name -p no:cacheprovider
